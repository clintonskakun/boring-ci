#!/bin/bash

# 1. Install sqlite3, docker-ce, go compiler
# 2. Create boring-ci user and group and add it to docker
# 3. Compile boring ci and add it to bin
# 4. Add /etc/boring-ci/config.json
# 4. Add systemd service file
